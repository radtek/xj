package com.gafis.xj.service;

import com.gafis.xj.model.ReportInfo;

/**
 * 更新程序管理接口
 * @author sun
 *
 */
public interface IReportInfoService {
	
	/**
	 * 根据id获取文件信息
	 * @param pkId
	 * @return
	 */
	ReportInfo findReportInfoById(String pkId);
	
	/**
	 * 保存上报信息
	 * @param ReportInfo
	 */
	int saveReportInfo(ReportInfo ReportInfo);
	
	 
	/**
	 * 保存导入的上报信息
	 * @param reportStr
	 * @return
	 */
	void saveImportReport(String reportStr);
}
